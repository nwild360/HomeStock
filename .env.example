# This is an example .env file for the Homestock application.
# Copy this file to .env and fill in the required values.

# -- Database configuration
POSTGRES_USER=homestock_app
POSTGRES_PASSWORD=change_me_now
POSTGRES_DB=homestock
POSTGRES_HOST=db
POSTGRES_PORT=5432

# -- Backend configuration
#
# Environment mode (development or production)
# In production mode, API documentation endpoints (/docs, /redoc, /openapi.json) are disabled
ENVIRONMENT=development

# CORS origins (browser's perspective, not container name)
# Development: http://localhost:5173
# Production: https://homestock.mydomain.net
CORS_ORIGINS=http://localhost:5173

# Cookie security settings
# Set to false for development (HTTP), true for production (HTTPS)
COOKIE_SECURE=false
COOKIE_SAMESITE=lax #Set to strict for prod, lax for dev

# Note: JWT signing uses dynamically generated cryptographic keys
# - RSA-2048 for RS256 (classical security)
# - Dilithium3 for post-quantum security (FIPS 204)
# Keys are ephemeral and regenerated on container restart

# -- Frontend configuration
#
# API URL for frontend to reach backend
# Development: http://localhost:8000
# Production: (empty string to use relative URLs like /api/auth/token)
VITE_API_URL=http://localhost:8000